<div class= "container">
<%= simple_form_for(@flavor) do |f| %>
  <% if flavor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flavor.errors.count, "error") %> prohibited this flavor from being saved:</h2>

      <ul>
      <% flavor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-inputs">
    <%= f.input :name, :prompt => "Select One" %>
  <div class="input-field">
    <%= f.label :active, class: "active" %>
    <%= f.select :active, options_for_select([['active', true], ['not active', false]]), :prompt => "Select One" %>
  </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
</div>
